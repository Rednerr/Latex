Задание 1:
 В рамках данного задания необходимо преобразовать текст своего варианта, представленный в файле .txt, в формат LaTeX, оформив его в соответствии с образцом, приведённым в .pdf .

.txt файлы содержат в себе текст в кодировке Windows-1251. Изучите команду `iconv`, чтобы изменить кодировку на UTF-8.

Требования к обработке текста

Исходный текст представляет собой набор рассказов. 

Необходимо разбить текст на абзацы (добавить пустые строки между абзацами), удалить разрывы слов (переносы), привести пунктуацию к стандартам LATEX
 для русского языка (кавычки: << >>; тире: "--- , "--~ , "--* (в зависимости от контекста); дефисы: "=; многоточие: \dots)
 
Каждый рассказ (без заголовка) должен быть вынесен в отдельный .tex-файл.
Все рассказы должны подключаться в основные документы с помощью команды \input. Текст рассказов не должен дублироваться в основных файлах.
Формирование двух документов


Первый документ должен быть типа article и иметь название «Избранные произведения». В нём каждый рассказ оформляется как отдельная секция с использованием команды \section, причём сама команда \section должна находиться в основном файле, а текст рассказа подключаться из внешнего файла с помощью команды \input.

Второй документ должен быть типа book и также иметь название «Избранные произведения». В этом документе каждый рассказ оформляется как отдельная глава с использованием команды \chapter, которая должна находиться в основном файле, тогда как текст рассказа подключается через \input. При этом должны использоваться те же самые файлы с текстами рассказов, что и в документе типа article.

Задание 2: Используя шаблон template.tex, в котором подключены все необходимые библиотеки, воспроизведите текст из файла task02.pdf, соостветствующий вашему варианту.


В качестве результата выполнения задания загрузите ссылку на репозиторий с LATEX
-файлами для получившегося документа.

Репозиторий НЕ ДОЛЖЕН включать в себя временные файлы, результаты сборки проекта или иные файлы, не имеющие непосредственного отношения к проекту.

Задание 3:
1. В работе должно присутствовать не менее 3-х разных математических формул
2. В работе должно присутствовать не менее 2-х изображений
3. В работе должно присутствовать не менее 1-ой таблицы
4. Должен присутствовать программный код, оформленный с помощью minted 
5. Должен присутствовать список источников, используемых в рамках работы
6. Используйте шаблон СГУ для курсовых работ на LaTeX 

На что стоит обратить внимание:

1. При оформлении специальных символов следует использовать специальные последовательности символов

2. Код должен быть отформатирован с помощью настроек, которые были разобраны на паре и приведены в лекции на курсе.

3. Все упоминания переменных должны сопровождаться специальным моноширинным шрифтом (вспоминаем про \texttt, \verb и \mintinline).

4. В нужных местах необходимо проставить ссылки на источники. Обратите внимание, что нумерация источников может отличаться, потому что в данном случае вы работаете лишь с фрагментом курсовой. Однако, номер источника должен быть проставлен автоматически, а сама ссылка — кликабельной (вспоминаем про \cite).

5. Аналогичные п.3 замечания относительно ссылок на изображения и приложения

Для более быстрой и эффективной работы предлагаем придерживаться следующих советов:

1. Стоит установить стандартные настройки форматирования minted для языка С++ и Python с помощью команды \setminted.

2. По возможности стоит использовать команду вставки кода из файла \inputminted. В случае невозможности применения этой команды (например, когда одновременно упомянается лишь фрагмент кода), стоит скопировать нужный фрагмент и вставить в окружение minted.

3. Ссылки на изображения и приложения следует оформлять в формате \label{reftype:refname}, где reftype — тип ссылки. Например, формула — eq, изображение — fig, код — code, приложение — app (приведены примеры названий, ваши могут отличаться)

4. Не стоит допускать файлов размера более 500 строк, по возможности разбивайте проект на несколько файлов и подключайте их с помощью \input или \include

В качестве результата выполненного задания загрузите ссылку на Git-репозиторий с созданными  LATEX
-файлами.

Репозиторий должен включать в себя:

1. Исходные файлы проекта с расширением .tex, стилевой файл SCWorks.cls и файл библиографии BibTeX

2. Графические изображения, необходимые для корректной сборки проекта

3. Исходные файлы программного кода, использующиеся библиотекой minted
